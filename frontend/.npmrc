# react-leaflet@4 and react-leaflet-cluster@4 disagree on @react-leaflet/core
# version (4.x vs 3.x). Both work fine in practice — Render's strict npm
# refuses to resolve. legacy-peer-deps=true matches local npm 6 behavior.
legacy-peer-deps=true
